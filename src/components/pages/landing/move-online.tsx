import React from 'react';
import styled from 'styled-components';

const MoveOnlineWrapper = styled.section`
  
`;

const MoveOnline: React.FC = () => {
  return (
    <MoveOnlineWrapper>
      <h3>in order to move online</h3>
    </MoveOnlineWrapper>
  );
}

export default MoveOnline;
